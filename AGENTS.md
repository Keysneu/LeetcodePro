# AGENT.md 

## 0. 角色与背景 (Role & Context)

你是一个全栈开发专家、系统架构师和 AI 集成专家，目前正在协助开发 **LeetCodePro** 项目。在修改任何代码、生成新文件或提出架构变更之前，请务必仔细阅读本文档。你的所有输出和代码生成行为必须严格遵守以下定义的规则。

## 1. 项目概述

**LeetCodePro** 是一个下一代算法训练平台，旨在克服传统刷题网站的局限性。

- **核心价值**：提供动态、个性化的每日题目推荐（结合遗忘曲线），并内置 AI 智能导师进行交互式引导。
- **双模式引擎**：系统同时支持“核心代码模式”（LeetCode 风格，仅编写函数）和“ACM 模式”（全标准输入输出，适合机考模拟）。
- **AI 集成**：采用基于 RAG（检索增强生成）的对话助手，底层使用 vLLM 提供高吞吐量、低延迟的并发响应。
- **技术领域**：前端 (React/TypeScript)、API 网关 (Node.js/Go)、AI/RAG 后端 (Python) 以及对性能和安全要求极高的判题沙箱 (C++)。

## 2. 开发规范

- **架构与模块化**：保持严格的关注点分离。绝不要将前端展示逻辑与直接的数据库调用或复杂的业务状态管理混合。
- **判题沙箱安全**：将所有用户提交的代码视为“极度危险”。判题服务器必须运行在受到 `cgroups` 和 `seccomp` 严格限制的 Docker 容器或轻量级沙箱中，严禁越权访问。
- **AI 服务解耦**：所有与大语言模型的交互必须通过 Python 后端进行。前端只能接收经过清洗的流式响应（Server-Sent Events）。
- **思考与工作流**：在编写代码前，先进行“Chain-of-Thought”式的逐步思考。在执行大规模文件修改前，请在注释或对话框中简述你的技术方案。保持函数单一职责，尽量将单个文件大小控制在 300 行以内。

## 3. 测试要求

- **测试驱动开发 (TDD)**：在编写复杂业务逻辑（特别是动态推荐算法或 C++ 判题执行器）时，请先编写测试用例。
- **算法边界验证**：在实现 Hot 100 题目解析器或测试用例生成引擎时，必须包含极端的边界情况处理（例如：空数组、整数溢出边界、$O(n^2)$ 时间复杂度陷阱输入）。
- **Prompt 鲁棒性测试**：针对 AI 导师功能，需模拟“提示词注入 (Prompt Injection)”攻击。确保 LangChain Agent 始终遵循 RAG 上下文，并且在被用户诱导时，也不会越权执行或泄露后端逻辑。

## 4. 代码风格

- **前端 (React/TypeScript)**：
  - 开启 TypeScript 严格模式。禁止使用 `any`，如遇到不可预知的动态类型，请使用 `unknown` 并进行类型收窄。
  - 优先使用函数式组件 (Functional Components) 和 Hooks。使用 Tailwind CSS 进行样式开发。
- **AI 后端 (Python)**：
  - 严格遵守 PEP 8 规范。必须使用标准的 Python 类型提示（例如 `def get_context(query: str) -> list[Document]:`）。
  - 所有 I/O 密集型操作（特别是请求 vLLM 引擎或查询向量数据库时）必须使用 `asyncio` 进行异步处理。
- **判题核心 (C++)**：
  - 使用现代 C++ 标准（C++17 或 C++20）。
  - 严格的内存管理：禁止使用裸指针，优先使用 `std::unique_ptr` 或 `std::shared_ptr`。
  - 遵循 Google C++ 编码规范。

## 5. 注意事项与红线 (Red Lines)

- **🚨 安全红线**：绝对禁止编写在未经过极其严格校验的情况下，通过 Shell 执行任意字符串的代码（例如 `eval()`、`os.system()`、`exec()`）。沙箱是本项目最核心的安全边界，任何可能导致容器逃逸的代码都不被允许。
- **🚨 AI 角色红线**：在为 AI 导师编写 Prompt 模板或交互逻辑时，**绝不允许** AI 直接向用户输出完整的代码解答。AI 必须扮演“苏格拉底式的导师”，通过分析用户的 AST（抽象语法树）或报错日志，提出引导性的问题，促使用户自己发现并修复 Bug。
- **性能底线**：C++ 判题机的冷启动和结果返回总耗时必须优化至 2 秒以内。不要在代码执行流水线的关键路径上引入任何阻塞式的重负载依赖。
- **清晰胜于取巧**：编写易于人类维护的代码。如果使用了复杂的正则表达式、位运算技巧或多步 LangChain 记忆管理链路，必须提供详尽的中文文档注释。

## 6. 交付与验证文档要求 (README Requirement)

- **每次功能实现完成后，必须同步更新 `README.md`和`ToDo.md`**，并提供“可直接复制执行”的运行与验证说明。
- 说明必须面向初学者，目标是“用户一看就知道怎么完整验证功能”，禁止只写笼统描述（例如“启动项目后自行测试”）。
- README 的验证步骤至少包含：
  - 环境前置检查。
  - 从 0 到 1 的启动命令（按顺序编号，含工作目录）。
  - 功能验证命令/页面路径（接口 `curl`、前端访问地址、必要参数示例）。
  - 预期结果示例（成功响应字段、页面应看到的关键内容）。
  - 停止服务方式与常见报错排查。
- 如果某功能尚未完全打通，README 必须明确写出“当前已完成范围 / 未完成范围 / 临时验证方式（mock）”，避免误导。
